// Widget tests for Mwanga Wallet.
//
// These are thin smoke tests that verify the app compiles without errors.
// Full integration tests require a running backend and are covered by the
// Spring Boot Testcontainers suite.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app compiles', () {
    expect(1 + 1, 2);
  });
}
